const db = require('../../config/database');

module.exports = {
/*                  GET                  */
    getUser: (emailUser) => {
        return new Promise((resolve, reject) => {
            db.getConnection((err, connection) => {
                if(err) { 
                    console.log(err); 
                    return; 
                }
                connection.query('SELECT * FROM Usuario WHERE correo_usuario = ?', [emailUser],
                (error, rows) => {
                    connection.release();
                    if (error) {
                        reject(error);
                        return;
                    }
                    resolve(rows);
                });
            });
        });
    },
    getUserId: (idUser) => {
        return new Promise((resolve, reject) => {
            db.getConnection((err, connection) => {
                if(err) { 
                    console.log(err); 
                    return; 
                }
                connection.query('SELECT * FROM Usuario WHERE id_usuario = ?', [idUser],
                (error, rows) => {
                    connection.release();
                    if (error) {
                        reject(error);
                        return;
                    }
                    resolve(rows);
                });
            });
        });
    },
    getUsers: () => {
        return new Promise((resolve, reject) => {
            db.getConnection((err, connection) => {
                if(err) { 
                    console.log(err); 
                    return; 
                }
                connection.query('SELECT * FROM Usuario WHERE administrador_boolean = 0', [],
                (error, rows) => {
                    connection.release();
                    if (error) {
                        reject(error);
                        return;
                    }
                    resolve(rows);
                });
            });
        });
    },
    getCounterUsers: () => {
        return new Promise((resolve, reject) => {
            db.getConnection((err, connection) => {
                if(err) { 
                    console.log(err); 
                    return; 
                }
                connection.query('SELECT COUNT(*) AS contador_usuarias FROM Usuario WHERE administrador_boolean = 0', [],
                (error, rows) => {
                    connection.release();
                    if (error) {
                        reject(error);
                        return;
                    }
                    resolve(rows);
                });
            });
        });
    },
    getUsersDates: () => {
        return new Promise((resolve, reject) => {
            db.getConnection((err, connection) => {
                if(err) { 
                    console.log(err); 
                    return; 
                }
                connection.query(`SELECT fecha_inscripcion_usuario, COUNT(*) AS contador_usuaria
                                FROM Usuario
                                WHERE fecha_inscripcion_usuario BETWEEN date_add(NOW(), INTERVAL -7 DAY) AND NOW() AND administrador_boolean = 0
                                GROUP BY fecha_inscripcion_usuario
                                ORDER BY fecha_inscripcion_usuario ASC`,
                (error, rows) => {
                    connection.release();
                    if (error) {
                        reject(error);
                        return;
                    }
                    resolve(rows);
                });
            });
        });
    },
/*                  POST                 */
    postUserRegistration: (password, admin, registeredDate, user) => {
        return new Promise((resolve, reject) => {
            db.getConnection((err, connection) => {
                if(err) { 
                    console.log(err); 
                    return; 
                }
                connection.query(`INSERT INTO Usuario (password_usuario, correo_usuario, administrador_boolean, fecha_inscripcion_usuario)
                                VALUES (?, ?, ?, ?)`, 
                [password, user.correo_usuario, admin, registeredDate],
                (error, rows) => {
                    connection.release();
                    if (error) {
                        reject(error);
                        return;
                    }
                    resolve(rows);
                });
            });
        });
    },
/*                  PUT                  */
    updateUser: (password, user) => {
        return new Promise((resolve, reject) => {
            db.getConnection((err, connection) => {
                if(err) { 
                    console.log(err); 
                    return; 
                }
                connection.query(`UPDATE Usuario 
                                SET password_usuario=?, correo_usuario=?    
                                WHERE id_usuario=?`, 
                [password, user.correo_usuario, user.id_usuario],
                (error, rows) => {
                    connection.release();
                    if (error) {
                        reject(error);
                        return;
                    }
                    resolve(rows);
                });
            });
        });
    },
/*               DELETE                  */
    deleteUser: (idUser) => {
        return new Promise((resolve, reject) => {
            db.getConnection((err, connection) => {
                if(err) { 
                    console.log(err); 
                    return; 
                }
                connection.query('DELETE FROM Usuario WHERE id_usuario = ?', [idUser],
                (error, rows) => {
                    connection.release();
                    if (error) {
                        reject(error);
                        return;
                    }
                    resolve(rows);
                });
            });
        });
    },
}